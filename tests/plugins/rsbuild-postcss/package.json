{
    "private": true,
    "scripts": {
        "test": "vitest run"
    },
    "dependencies": {
        "ammolite": "workspace:*",
        "vitest": "catalog:dep"
    },
    "devDependencies": {
        "@ammolite/postcss": "workspace:*",
        "@ammolite/rsbuild": "workspace:*",
        "@rsbuild/core": "catalog:dev"
    }
}
